<%= label_tag 'txtBusca', 'Pesquisar' %>
<%= text_field_tag 'txtBusca', '', :class => 'span11', :id => 'txtBusca' %>
<div id='div_color'>
  <table  id="ulItens" class="table table-striped table-hover table-bordered">
    <colgroup>
      <col>
      <col width="160">
    </colgroup>
    <thead>
      <tr>
        <th>Selecione o usuários para ver mais detalhes</th>
      </tr>
    </thead>
    <tbody>
      <% if @managers.any? %>
        <% @managers.each do |manager| %>
          <tr>
            <td align='center'>
                <%= link_to "#{manager}", edit_manager_path(manager), :class => "", :title => "Editar #{manager}"  %>
            </td>
          </tr>
        <% end %>
      <% else %>
      <tr>
        <td colspan='2'>Nenhum registro foi encontrado.</td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div class="form-actions">
  <%= link_to 'Novo Usuário', new_manager_path, :class => "btn btn-success" %>
</div>
	<% @calendar.days.group_by(&:month_string).each do |month_string, days| %>
		 <h4 class="text-success"><%= month_string %></h4>
		<table class='table'>
			<tr>
				<td>
				 <h4 class="text-success">D</h4>
				</td>
				<td>
				 <h4 class="text-success">S</h4>
				</td>
				<td>
				 <h4 class="text-success">T</h4>
				</td>
				<td>
				 <h4 class="text-success">Q</h4>
				</td>
				<td>
				 <h4 class="text-success">Q</h4>
				</td>
				<td>
				 <h4 class="text-success">S</h4>
				</td>
				<td>
				 <h4 class="text-success">S</h4>
				</td>				
			</tr>
			<tr>
				<td>
					<% days.sort_by(&:week).each do |d| %>
						<% if d.wday ==  DayWeek::SUNDAY %>
						<p>
							 <h4 class="text-error"><%= link_to "#{d.day.day}", d  %><% if d.day == Date.current %>*<%end%></h4>
						</p>
						<% end %>
				  <% end %>
				</td>
				<td>
					<% days.sort_by(&:week).each do |d| %>					
						<% if d.wday ==  DayWeek::MONDAY %>
							<p>
								 <h4 class="text-error"><%= link_to "#{d.day.day}", d  %><% if d.day == Date.current %>*<%end%></h4>
							</p>
						<% end %>
				  <% end %>					
				</td>
				<td>
					<% days.sort_by(&:week).each do |d| %>
						<% if d.wday ==  DayWeek::TUESDAY %>
						<p>
							 <h4 class="text-error"><%= link_to "#{d.day.day}", d  %><% if d.day == Date.current %>*<%end%></h4>
						</p>
						<% end %>
				  <% end %>					
				</td>
				<td>
					<% days.sort_by(&:week).each do |d| %>
						<% if d.wday ==  DayWeek::WEDNESDAY %>
						<p>
							 <h4 class="text-error"><%= link_to "#{d.day.day}", d  %><% if d.day == Date.current %>*<%end%></h4>
						</p>
						<% end %>
				  <% end %>										
				</td>
				<td>
					<% days.sort_by(&:week).each do |d| %>
						<% if d.wday ==  DayWeek::THURSDAY %>
						<p>
							 <h4 class="text-error"><%= link_to "#{d.day.day}", d  %><% if d.day == Date.current %>*<%end%></h4>
						</p>
						<% end %>
				  <% end %>															
				</td>
				<td>
					<% days.sort_by(&:week).each do |d| %>
						<% if d.wday ==  DayWeek::FRIDAY %>
						<p>
							 <h4 class="text-error"><%= link_to "#{d.day.day}", d  %><% if d.day == Date.current %>*<%end%></h4>
						</p>
						<% end %>
				  <% end %>																				
				</td>
				<td>
					<% days.sort_by(&:week).each do |d| %>
						<% if d.wday ==  DayWeek::SATURDAY %>
						<p>
							 <h4 class="text-error"><%= link_to "#{d.day.day}", d  %><% if d.day == Date.current %>*<%end%></h4>
						</p>
						<% end %>
				  <% end %>																				
				</td>
			</tr>
		</table>
	<% end %>
<a id='open' href="#">Calendário</a>
<a id='close' href="#">x</a>
<% contador = 0 %>
<table id='information' class="table table-striped table-hover table-bordered">
  <% @all_days.group_by(&:month_string).each do |month_string, days|%>
      <% if contador == 0 %>
        <tr>
        <%end%>
        <%  contador = contador + 1 %>
        <td>
          <div class="alert  alert-info">
            <p class='text-center'><strong><%= month_string %></strong><br></p>
                <% days.sort_by(&:wday).group_by(&:weekday_less).each do |weekday_less, days| %>
                  <div class="alert  alert-info">
                  </div>
<%= weekday_less%>

                <%end%>
            </td>
        <% if contador == 2 %>
          <%contador = 0%>
        </tr>
        </div>
      <%end%>
   <% end %>
  </table>
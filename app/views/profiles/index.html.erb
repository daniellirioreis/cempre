<blockquote>
  <table class='table'>
    <tr>
      <td>
        <%= link_to new_profile_path, role: 'button' do %>
          <h2 class="text-success" title='Novo Perfil'><i class="icon-plus"></i></h2>
        <% end %>
      </td>
	 </tr>
	</table>
</blockquote>
<%= label_tag 'txtBusca', 'Pesquisar' %>
<%= text_field_tag 'txtBusca', '', :class => 'span11 input-search', :id => 'txtBusca',:placeholder => "Buscar nesta lista", :alt =>"list_profiles" %>

<table class="list_profiles table table-striped table-hover table-bordered">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Ação</th>
        </tr>
    </thead>
    <tbody>
        <% if @profiles.any? %>
          <% @profiles.each do |s| %>
            <tr>
                <td align="center">
                  <%= s %>
                </td>
                <td>
                  <div class="btn-group">
                    <%= link_to "editar", edit_profile_path(s), :class => "btn btn-primary" %>
                  </div>
                </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan='2'>Nenhum registro foi encontrado.</td>
          </tr>
        <% end %>
    </tbody>
</table>

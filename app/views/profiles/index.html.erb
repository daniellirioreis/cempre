<%= label_tag 'txtBusca', 'Pesquisar' %>
<%= text_field_tag 'txtBusca', '', :class => 'span11', :id => 'txtBusca' %>
<div id='div_color'>
  <table  id="ulItens" class="table table-striped table-hover table-bordered">
    <colgroup>
      <col>
      <col width="160">
    </colgroup>
    <thead>
      <tr>
        <th>Selecione a perfis para ver mais detalhes</th>
      </tr>
    </thead>
    <tbody>
      <% if @profiles.any? %>
        <% @profiles.each do |profile| %>
          <tr>
            <td align='center'>
                <%= link_to "#{profile}", edit_profile_path(profile), :class => "", :title => "Editar #{profile}"  %>
            </td>
          </tr>
        <% end %>
      <% else %>
      <tr>
        <td colspan='2'>Nenhum registro foi encontrado.</td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div class="form-actions">
  <%= link_to 'Novo Perfil', new_profile_path, :class => "btn" %>
</p>
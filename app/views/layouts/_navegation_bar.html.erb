<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span>
        <span class="icon-bar"></span>
         <span class="icon-bar"></span>
      </a>
      <% if user_signed_in? %>
        <% if current_user.student? %>
        <%= link_to "#{ current_user.student.company_active  }", root_path, :class => "brand" %>
        <% else %>
          <%= link_to "#{current_company  } - #{current_calendar}", root_path, :class => "brand" %>
        <% end %>
      <% else %>
        <%= link_to "CEMPRE", root_path, :class => "brand" %>
      <% end %>

      <% if user_signed_in? %>
        <div class="nav-collapse collapse">
          <ul class="nav pull-right">
            <li class="dropdown">
              <a href="#" role="button" class="dropdown-toggle" data-toggle="dropdown">
                <i class="icon-user"></i>
                <%= current_user %>
                <i class="caret"></i>
              </a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to edit_manager_path(current_user), :tabindex => "-1" do %>
                    <i class="icon-pencil"></i>
                    Edit
                  <% end %>
                </li>
                <li>
                  <%= link_to change_companies_manager_path(current_user), :tabindex => "-1" do %>
                    <i class="icon-home"> </i><i class="icon-calendar"></i>
                      Trocar de escola/período
                  <% end %>
                </li>
              </ul>
            </li>
            <li>
              <%= link_to destroy_user_session_path, :data => { :confirm => I18n.t("helpers.confirmation.sure") }, :method => :delete, :tabindex => "-1" do %>
                <i class="icon-off"></i>
                Logout
              <% end %>
            </li>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</div>

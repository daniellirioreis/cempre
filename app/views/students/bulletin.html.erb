<%if @groups_approved.any? %>
  <% @groups_approved.each do |g| %>
    <%= g %>
  <% end %>
<% else %>
  <div class="alert alert-block">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
      Não existe boletins lançado.
  </div>
<%end%>

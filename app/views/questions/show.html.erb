<%= @question %><br/>
<%= @question.type_question %>

<div class="form-actions">
  <%= link_to 'Editar', edit_question_path(@question), class: 'btn btn-primary' %>
  <%= link_to 'Deletar', @question, method: :delete, data: { confirm: 'Confirma exclusão?'}, :class => "btn btn-danger" %>
  <%= link_to 'Voltar', :back, class: 'btn' %>
</div>

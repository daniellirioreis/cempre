<%= label_tag 'txtBusca', 'Pesquisar' %>
<%= text_field_tag 'txtBusca', '', :class => 'span11', :id => 'txtBusca' %>
<div id='div_color'>
  <table  id="ulItens" class="table table-striped table-hover table-bordered">
    <colgroup>
      <col>
      <col width="160">
    </colgroup>
    <thead>
      <tr>
        <th>Selecione a resposta para ver mais detalhes</th>
      </tr>
    </thead>
    <tbody>
      <% if @answers.any? %>
        <% @answers.each do |answer| %>
          <tr>
            <td align='center'>
                <%= link_to "#{answer}", answer, :class => "", :title => "Editar #{answer}"  %>
            </td>
          </tr>
        <% end %>
      <% else %>
      <tr>
        <td colspan='2'>Nenhum registro foi encontrado.</td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div class="form-actions">
  <%= link_to 'Novo Resposta', new_answer_path, :class => "btn" %>
</p>